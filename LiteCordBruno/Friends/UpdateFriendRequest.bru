meta {
  name: UpdateFriendRequest
  type: http
  seq: 3
}

post {
  url: {{rocket_url}}:{{rocket_port}}/friends/update_friend_request/{{friendship_id}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
