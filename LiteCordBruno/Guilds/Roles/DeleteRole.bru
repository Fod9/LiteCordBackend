meta {
  name: DeleteRole
  type: http
  seq: 3
}

delete {
  url: {{rocket_url}}:{{rocket_port}}/guilds/{{guild_id}}/roles/{{role_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
