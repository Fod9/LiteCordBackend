meta {
  name: ListChannels
  type: http
  seq: 2
}

get {
  url: {{rocket_url}}:{{rocket_port}}/guilds/{{guild_id}}/channels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
