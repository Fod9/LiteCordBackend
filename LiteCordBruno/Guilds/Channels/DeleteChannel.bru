meta {
  name: DeleteChannel
  type: http
  seq: 3
}

delete {
  url: {{rocket_url}}:{{rocket_port}}/guilds/{{guild_id}}/channels/{{channel_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
